<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nursinghomecarebd.www.nursinghomecare">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MedicalActivity"></activity>
        <activity android:name=".Activities.OxygenActivity" />
        <activity
            android:name=".Activities.BabyActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activities.DoctorActivity" />
        <activity android:name=".Activities.LocationActivity" />
        <activity android:name=".Activities.ContractActivity" />
        <activity android:name=".Activities.Feedback" />
        <activity
            android:name=".Activities.AboutActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activities.TermActivity" />
        <activity android:name=".Activities.PrivacyActivity" />
        <activity android:name=".Activities.LoginActivity" />
        <activity
            android:name=".Activities.Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.WelcomeScreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.HomeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activities.RegisterActivity" />
    </application>

</manifest>